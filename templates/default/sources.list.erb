# Generated by Chef
<%
# Have to enclose node['debian']['mirror'] in double-quote in case URL contains spaces characters
mirror_uri = '"' + node['debian']['mirror'] + '"' unless node['debian']['mirror'].start_with?('"', "'")
repo = "#{mirror_uri} #{node['debian']['codename']} #{node['debian']['components'].join(' ')}"
-%>

deb <%= repo %>
<% if node['debian']['deb_src'] -%>
deb-src <%= repo %>
<% end -%>
